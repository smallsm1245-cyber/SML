# 한글 폰트 추가 가이드

## 📌 현재 상황
PDF 생성은 작동하지만, 한글이 깨져서 표시됩니다. 한글 폰트 Base64 데이터를 추가해야 합니다.

## 🚀 빠른 해결 방법 (5분 소요)

### 1단계: Noto Sans KR 폰트 다운로드

Google Fonts에서 다운로드:
1. https://fonts.google.com/noto/specimen/Noto+Sans+KR 접속
2. 우측 상단 "Download family" 클릭
3. ZIP 파일 다운로드 및 압축 해제
4. **`NotoSansKR-Regular.ttf`** 파일 찾기

### 2단계: TTF 파일을 Base64로 변환

**온라인 변환기 사용:**
1. https://rawgit.com/MrRio/jsPDF/master/fontconverter/fontconverter.html 접속
   (또는 다른 Base64 인코더: https://www.giftofspeed.com/base64-encoder/)
2. `NotoSansKR-Regular.ttf` 파일 업로드
3. "Create" 버튼 클릭
4. 생성된 JavaScript 코드 복사

### 3단계: pdf-module.js에 Base64 추가

생성된 코드에서 Base64 문자열을 복사하여 `pdf-module.js`의 25번째 줄을 다음과 같이 수정:

```javascript
// 변경 전
const NOTO_SANS_KR_BASE64 = null;

// 변경 후
const NOTO_SANS_KR_BASE64 = "여기에_생성된_Base64_문자열_붙여넣기";
```

### 4단계: 파일 저장 및 테스트

1. `pdf-module.js` 파일 저장
2. 브라우저 새로고침
3. PDF 다운로드 다시 시도

---

## 🎯 자동화 스크립트 (Node.js 사용)

Node.js가 설치되어 있다면 자동으로 변환할 수 있습니다:

### 스크립트 파일 생성

프로젝트 루트에 `convert-font.js` 생성:

```javascript
const fs = require('fs');
const path = require('path');

// Noto Sans KR TTF 파일 경로 (다운로드한 파일 경로로 변경)
const fontPath = './NotoSansKR-Regular.ttf';
const outputPath = './js/noto-sans-kr-base64.js';

// TTF 파일을 Base64로 변환
const fontBuffer = fs.readFileSync(fontPath);
const base64Font = fontBuffer.toString('base64');

// JavaScript 파일로 저장
const jsContent = `// Noto Sans KR Regular Font (Base64)
// Auto-generated by convert-font.js

export const NOTO_SANS_KR_BASE64 = "${base64Font}";
`;

fs.writeFileSync(outputPath, jsContent);
console.log('✅ 폰트 변환 완료:', outputPath);
console.log('📊 Base64 크기:', Math.round(base64Font.length / 1024), 'KB');
```

### 실행

```bash
# Noto Sans KR Regular.ttf 파일을 프로젝트 루트에 복사 후
node convert-font.js
```

### pdf-module.js 수정

생성된 `noto-sans-kr-base64.js`를 불러오도록 수정하거나, Base64 문자열을 직접 복사

---

## ⚡ 더 간단한 대안 (경량화)

전체 폰트 대신 **자주 사용하는 한글만 포함한 서브셋 폰트** 사용:

### 서브셋 폰트 생성기
- https://transfonter.org/ 접속
- "Subsetting" 옵션에서 한글 범위만 선택
- 파일 크기를 약 50% 줄일 수 있음

---

## ❓ 문제 해결

### Q: Base64 문자열이 너무 큽니다 (2-3MB)
A: 서브셋 폰트를 사용하거나, 외부 파일로 분리하세요.

### Q: 변환 후에도 한글이 깨집니다
A: 
1. 브라우저 캐시 삭제 후 새로고침 (Ctrl+Shift+R)
2. 콘솔에서 에러 메시지 확인
3. `setupKoreanFont()` 함수가 정상 실행되는지 확인

### Q: 다른 폰트를 사용하고 싶습니다
A: 동일한 방법으로 다른 TTF 폰트를 변환하여 사용 가능합니다.

---

## 📝 참고

- 폰트 파일 크기: 약 2-3MB → Base64: 약 3-4MB
- 페이지 로딩 시간에 영향을 줄 수 있음
- 프로덕션 환경에서는 CDN이나 외부 호스팅 고려
