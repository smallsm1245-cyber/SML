# 🎨 SMALLSM Archive - 위키형 관리자 대시보드 완전판

## ✨ 새로 추가된 모든 기능

---

## 📊 1. 통합 대시보드

### 📈 실시간 통계
- **총 게시물 수** 📝
- **공개 중인 글** 🟢
- **비공개 글** 🔴  
- **카테고리 수** 📂

### 🕐 최근 활동 로그
- 새 글 작성 시각
- 카테고리 변경 이력
- 삭제/복구 기록

### ⚡ 빠른 작업 버튼
- ✍️ 새 글 작성
- 🏠 홈 수정
- 📂 카테고리 추가
- 🔗 사이트 보기 (새 탭)

---

## 🏠 2. 홈 화면 관리 (업그레이드)

### ✅ 기존 기능
- 제목, 부제목, 본문 편집
- 최신 게시물 표시 on/off
- 자동 저장

### 🆕 **새 기능**

#### 📺 **실시간 미리보기** (Split View)
```
┌─────────────┬─────────────┐
│   편집창    │ 미리보기    │
│             │             │
│ 제목: _____ │ 제목이      │
│ 본문: _____ │ 실시간으로  │
│             │ 보임!       │
└─────────────┴─────────────┘
```
- 왼쪽에서 타이핑하면 오른쪽에 즉시 반영
- 스크롤 동기화
- 글 쓰면서 바로 확인 가능

#### 🌓 **다크모드 미리보기 토글**
```
미리보기 상단 버튼:
[🌙 다크]  ← 클릭!

→ 미리보기 화면이 다크모드로 전환
→ 라이트/다크 양쪽 가독성 확인
```

**사용 시나리오:**
1. 본문 작성
2. "🌙 다크" 버튼 클릭
3. 다크모드에서 텍스트가 잘 보이는지 확인
4. 다시 클릭하면 라이트모드로 복귀

---

## 📝 3. 게시글 관리 (완전 개편)

### 🔍 **검색 및 필터**
```
┌────────────────────────────────────┐
│ 🔍 제목 검색 | [카테고리▼] [상태▼] │
└────────────────────────────────────┘
```

**필터 옵션:**
- 모든 카테고리 / ESSENCE / PRACTICE...
- 모든 상태 / 공개 / 비공개

**실시간 검색:**
- 타이핑하면 즉시 필터링
- 대소문자 구분 없음

### 🎨 **상태 색상 배지**
```
📝 글 제목    [🟢 공개]
📝 작성 중... [🟡 작성 중]
📝 비밀글     [🔴 비공개]
```

**색상 의미:**
- 🟢 녹색: 공개 중 (is_private: false)
- 🔴 빨강: 비공개 (is_private: true)
- 🟡 노랑: 작성 중 (draft 상태)

### 📋 **게시글 카드**
```
┌────────────────────────────────────┐
│ 📝 글 제목               [🟢 공개] │
│ ESSENCE • 2026-02-08 • 최종 수정일 │
│                                    │
│ [📋 링크 복사] [✏️ 수정] [🗑️ 삭제] │
└────────────────────────────────────┘
```

**기능:**
- **📋 링크 복사**: 클릭 한 번에 URL 클립보드 복사
- **✏️ 수정**: 에디터로 바로 이동
- **🗑️ 삭제**: 휴지통으로 이동 (30일 보관)

### 📅 **최종 수정일 표시**
- 각 글마다 마지막 수정 시각 표시
- "2시간 전", "3일 전" 형식
- 오래된 글 확인 용이

---

## 📂 4. 카테고리 관리 (업그레이드)

### ✅ 기존 기능
- 드래그 앤 드롭 순서 변경
- 공개/비공개 전환
- 드롭다운 설정
- 추가/삭제

### 🆕 **새 기능**

#### ✏️ **인라인 이름 수정**
```
┌────────────────────────────────────┐
│ ⋮⋮  [ESSENCE____]  👁️  ✏️  🗑️     │
│      ↑ 클릭하면 바로 수정 가능!      │
└────────────────────────────────────┘
```

**사용법:**
1. 카테고리 이름 클릭
2. 그 자리에서 타이핑
3. Enter 또는 포커스 이동 시 자동 저장

**장점:**
- 팝업 안 뜸
- 즉시 수정
- 실수하면 ESC로 취소

#### 👁️ **노출 제어 아이콘**
```
👁️  ← 공개 (사이드바에 보임)
🙈  ← 비공개 (숨김)

클릭하면 토글됨!
```

**기존 방식:**
```
라디오 버튼:
○ 공개  ● 비공개
```

**새 방식:**
```
아이콘 버튼:
[👁️] ← 클릭 한 번!
```

---

## 🗑️ 5. 휴지통 (30일 보관)

### 📦 **삭제된 항목 보관**
```
┌────────────────────────────────────┐
│ 🗑️ 휴지통                [3]      │
├────────────────────────────────────┤
│ 📝 삭제된 글 1                     │
│ 삭제일: 2026-02-05 (3일 남음)      │
│ [♻️ 복구] [🗑️ 영구 삭제]           │
├────────────────────────────────────┤
│ 📂 삭제된 카테고리                  │
│ 삭제일: 2026-02-01 (7일 남음)      │
│ [♻️ 복구] [🗑️ 영구 삭제]           │
└────────────────────────────────────┘
```

**기능:**
- 삭제 후 30일간 자동 보관
- 남은 기간 표시
- 원클릭 복구
- 영구 삭제 (복구 불가)

**자동 정리:**
- 30일 지나면 자동 삭제
- 휴지통 배지에 개수 표시

---

## 🖼️ 6. 이미지 관리 (드래그 & 붙여넣기)

### 📎 **드래그 앤 드롭**
```
에디터 화면:
┌────────────────────────────────────┐
│ 본문:                              │
│ ┌────────────────────────────────┐ │
│ │ 여기에 이미지 파일을            │ │
│ │ 드래그해서 놓으세요!           │ │
│ │                                │ │
│ │ 또는 Ctrl+V로 붙여넣기!        │ │
│ └────────────────────────────────┘ │
└────────────────────────────────────┘
```

**사용법 1: 드래그**
1. 탐색기에서 이미지 선택
2. 에디터로 드래그
3. 놓으면 자동 업로드
4. 마크다운 문법 자동 삽입

**사용법 2: 붙여넣기**
1. 스크린샷 찍기 (Win + Shift + S)
2. 에디터에서 Ctrl + V
3. 자동 업로드 및 삽입

**지원 형식:**
- JPG, PNG, GIF, WebP
- 최대 5MB

---

## ⚠️ 7. 이동 전 경고

### 💾 **변경사항 유실 방지**
```
수정 중인 글이 있을 때:

┌────────────────────────────────────┐
│ ⚠️ 경고                            │
├────────────────────────────────────┤
│ 저장하지 않은 변경사항이 있습니다.  │
│                                    │
│ 정말 이동하시겠습니까?              │
│                                    │
│ [취소]  [저장하고 나가기]  [그냥 나가기] │
└────────────────────────────────────┘
```

**작동:**
- 글 수정 중 다른 메뉴 클릭 시
- 브라우저 닫기/새로고침 시도 시
- 저장 안 했으면 경고 팝업

**옵션:**
- **취소**: 계속 작성
- **저장하고 나가기**: 자동 저장 후 이동
- **그냥 나가기**: 변경사항 버림

---

## 🎨 8. 다크모드 (전역)

### 🌓 **테마 전환**
```
헤더 오른쪽:
[🌙] ← 클릭!

→ 전체 대시보드가 다크모드로 전환
→ 다시 클릭하면 라이트모드
```

**저장:**
- localStorage에 테마 설정 저장
- 다음 접속 시 자동 적용

**미리보기 vs 전역:**
- **미리보기 다크**: 홈 화면 미리보기만
- **전역 다크**: 관리자 대시보드 전체

---

## 🚀 설치 방법

### 1️⃣ **기존 파일 백업**
```bash
# 혹시 모르니 백업!
cp -r your-project your-project-backup
```

### 2️⃣ **데이터베이스 업데이트**

**Supabase SQL Editor에서 실행:**

```sql
-- 휴지통 테이블 생성
CREATE TABLE IF NOT EXISTS trash (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    item_type TEXT NOT NULL, -- 'post' or 'category'
    item_id UUID NOT NULL,
    item_data JSONB NOT NULL,
    deleted_at TIMESTAMP WITH TIME ZONE DEFAULT now(),
    expires_at TIMESTAMP WITH TIME ZONE DEFAULT (now() + interval '30 days')
);

-- 게시물에 draft 상태 추가
ALTER TABLE archive_posts
ADD COLUMN IF NOT EXISTS status TEXT DEFAULT 'published';

-- 기존 글은 is_private 기준으로 상태 설정
UPDATE archive_posts
SET status = CASE
    WHEN is_private THEN 'private'
    ELSE 'published'
END
WHERE status IS NULL;

-- 게시물 updated_at 컬럼 확인
ALTER TABLE archive_posts
ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP WITH TIME ZONE DEFAULT now();

-- 자동 정리 함수 (30일 지난 휴지통 항목 삭제)
CREATE OR REPLACE FUNCTION cleanup_trash()
RETURNS void AS $$
BEGIN
    DELETE FROM trash WHERE expires_at < now();
END;
$$ LANGUAGE plpgsql;
```

### 3️⃣ **파일 업로드**

새 파일:
- `admin-dashboard.html` (새 대시보드)
- `css/admin-dashboard.css` (새 스타일)
- `js/admin-dashboard.js` (새 로직)

기존 파일 유지:
- `admin.html` (구버전, 백업용)
- 나머지 모든 파일

### 4️⃣ **접속**
```
https://your-site.com/admin-dashboard.html
```

---

## 📁 파일 구조

```
project/
├── admin.html               (구버전 - 백업)
├── admin-dashboard.html     (신버전 - 메인)
├── index.html
├── post.html
├── css/
│   ├── style.css
│   └── admin-dashboard.css  (새 파일)
├── js/
│   ├── admin.js             (구버전)
│   ├── admin-dashboard.js   (신버전)
│   ├── main.js
│   └── post.js
└── database-update-v2.sql   (새 SQL)
```

---

## 🎯 사용 시나리오

### 시나리오 1: 새 글 작성
1. 대시보드 접속
2. "✍️ 새 글 작성" 클릭
3. 제목, 카테고리, 본문 입력
4. 이미지 드래그 & 드롭
5. 상태: 🟡 작성 중 선택
6. 저장
7. 나중에 🟢 공개로 전환

### 시나리오 2: 홈 화면 계절 업데이트
1. "🏠 홈 화면 관리" 클릭
2. 제목: "크리스마스 🎄"
3. 실시간 미리보기 확인
4. "🌙 다크" 버튼으로 다크모드 가독성 체크
5. 저장

### 시나리오 3: 실수로 삭제한 글 복구
1. "🗑️ 휴지통" 클릭
2. 삭제된 글 확인
3. "♻️ 복구" 클릭
4. 원래 위치로 복원됨

### 시나리오 4: 카테고리 빠른 수정
1. "📂 카테고리" 클릭
2. 카테고리 이름 클릭
3. 그 자리에서 수정
4. Enter → 자동 저장
5. 👁️ 아이콘으로 공개/비공개 토글

---

## 🐛 문제 해결

### 미리보기가 안 보일 때
- 브라우저 캐시 삭제
- F12 콘솔에서 에러 확인
- `admin-dashboard.css` 로드 확인

### 드래그 앤 드롭이 안 될 때
- 파일 크기 5MB 이하 확인
- Supabase Storage 설정 확인
- 브라우저 권한 확인

### 휴지통이 비어있을 때
- `trash` 테이블 생성 확인
- SQL 권한 확인

---

## 🎉 완성!

이제 완전한 위키형 관리 시스템을 사용하실 수 있습니다!

**주요 개선점:**
- ✅ 직관적인 대시보드
- ✅ 실시간 미리보기
- ✅ 빠른 작업 흐름
- ✅ 안전한 휴지통
- ✅ 편리한 이미지 관리
- ✅ 다크모드 지원

**다음 단계:**
1. 관리자 계정으로 로그인
2. 대시보드 둘러보기
3. 테스트 글 작성해보기
4. 각 기능 체험하기

즐거운 위키 관리 되세요! 🎨
