# 🎨 Toast UI Editor 통합 - 완전 가이드

## ✨ 새로 추가된 기능

### 📝 **Toast UI Editor**
- ✅ 마크다운 & WYSIWYG 모드
- ✅ 실시간 미리보기 (Split View)
- ✅ 이미지 드래그 & 드롭 업로드
- ✅ 다양한 포맷팅 도구
- ✅ 코드 블록 하이라이팅
- ✅ 테이블, 링크, 이미지 삽입

### 📋 **게시글 관리 복구**
- ✅ 게시물 목록 불러오기
- ✅ 카테고리/상태 필터링
- ✅ 제목 검색
- ✅ 링크 복사 기능
- ✅ 수정/삭제 기능

### ✍️ **통합 에디터**
- ✅ 새 글 작성
- ✅ 기존 글 수정
- ✅ 임시 저장 (localStorage)
- ✅ 카테고리 선택
- ✅ 비밀글 설정
- ✅ 복사 출처 비활성화

---

## 🚀 설치 방법

### **1단계: 파일 업로드**

다음 파일들을 업로드하세요:

**신규 파일:**
- `admin-toastui.html` ← **새 관리자 페이지**
- `css/admin-toastui.css` ← **Toast UI 스타일**
- `js/admin-toastui.js` ← **Toast UI JavaScript**

**기존 파일 유지:**
- 나머지 모든 파일 그대로

### **2단계: 데이터베이스 확인**

기존에 다음 테이블들이 있어야 합니다:
- `archive_posts`
- `categories`
- `settings`
- `trash` (선택사항)

### **3단계: Storage 버킷 확인**

**Supabase Dashboard > Storage**에서:
- 버킷 이름: `archive-images`
- Public 설정: Yes
- 정책: 관리자만 업로드 가능

정책이 없다면 추가:
```sql
-- Insert policy
CREATE POLICY "Admin can upload images"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (
    bucket_id = 'archive-images' 
    AND auth.email() = 'your-admin-email@example.com'
);

-- Select policy
CREATE POLICY "Anyone can view images"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'archive-images');
```

### **4단계: 접속**

```
https://your-site.com/admin-toastui.html
```

---

## 📝 Toast UI Editor 사용법

### **기본 도구**

```
┌────────────────────────────────────────┐
│ H B I S | - " | • 1 ► ◄ | ⊞ 🖼 🔗 | < > │
└────────────────────────────────────────┘
  │ │ │ │   │ │   │ │ │ │   │  │  │   │ │
  │ │ │ │   │ │   │ │ │ │   │  │  │   └─ 코드블록
  │ │ │ │   │ │   │ │ │ │   │  │  └───── 링크
  │ │ │ │   │ │   │ │ │ │   │  └──────── 이미지
  │ │ │ │   │ │   │ │ │ │   └─────────── 테이블
  │ │ │ │   │ │   │ │ │ └─────────────── 내어쓰기
  │ │ │ │   │ │   │ │ └───────────────── 들여쓰기
  │ │ │ │   │ │   │ └─────────────────── 숫자목록
  │ │ │ │   │ │   └───────────────────── 불릿목록
  │ │ │ │   │ └───────────────────────── 인용구
  │ │ │ │   └─────────────────────────── 구분선
  │ │ │ └─────────────────────────────── 취소선
  │ │ └───────────────────────────────── 기울임
  │ └─────────────────────────────────── 굵게
  └───────────────────────────────────── 제목
```

### **이미지 업로드 3가지 방법**

#### 방법 1: 드래그 & 드롭
```
1. 에디터에 이미지 파일을 드래그
2. 에디터 위에서 놓기
3. 자동 업로드 → Supabase Storage
4. 마크다운 자동 삽입: ![Image](https://...)
```

#### 방법 2: 이미지 버튼
```
1. 툴바에서 🖼 버튼 클릭
2. 파일 선택 또는 URL 입력
3. 파일 선택 시 자동 업로드
```

#### 방법 3: 붙여넣기 (개발 예정)
```
1. 스크린샷 찍기 (Win + Shift + S)
2. 에디터에서 Ctrl + V
3. 자동 업로드
```

### **마크다운 모드 vs WYSIWYG 모드**

**마크다운 모드:**
```markdown
# 제목 1
## 제목 2

**굵게** *기울임* ~~취소선~~

- 불릿 1
- 불릿 2

[링크](https://example.com)
![이미지](url)
```

**WYSIWYG 모드:**
- 워드처럼 직접 편집
- 버튼 클릭으로 포맷팅
- 실시간 결과 확인

**전환:**
- 에디터 우측 상단에서 토글

---

## 📋 게시글 관리

### **목록 화면**

```
┌──────────────────────────────────────────────┐
│ 📝 게시글 관리            [✍️ 새 글 작성]    │
├──────────────────────────────────────────────┤
│ 🔍 [검색] [카테고리▼] [상태▼]               │
├──────────────────────────────────────────────┤
│ 📝 첫 번째 글 제목              [🟢 공개]   │
│ ESSENCE • 2시간 전                           │
│ [📋 링크 복사] [✏️ 수정] [🗑️ 삭제]          │
├──────────────────────────────────────────────┤
│ 📝 두 번째 글 제목              [🔴 비공개] │
│ PRACTICE • 1일 전                            │
│ [📋 링크 복사] [✏️ 수정] [🗑️ 삭제]          │
└──────────────────────────────────────────────┘
```

### **검색 및 필터**

**검색:**
- 제목으로 실시간 검색
- 대소문자 구분 없음

**카테고리 필터:**
- 모든 카테고리
- ESSENCE
- PRACTICE
- GLOSSARY
- ...

**상태 필터:**
- 모든 상태
- 공개 (🟢)
- 비공개 (🔴)

---

## ✍️ 글 작성 워크플로우

### **시나리오 1: 새 글 작성**

```
1. [✍️ 새 글 작성] 클릭
   ↓
2. 제목 입력
   ↓
3. 카테고리 선택
   ↓
4. Toast UI Editor에서 본문 작성
   - 마크다운 or WYSIWYG 선택
   - 이미지 드래그 & 드롭
   - 테이블, 링크 삽입
   ↓
5. 옵션 선택:
   ☐ 비밀글로 설정
   ☐ 복사 출처 비활성화
   ↓
6. [💾 임시 저장] 또는 [🚀 발행하기]
```

### **시나리오 2: 기존 글 수정**

```
1. 목록에서 [✏️ 수정] 클릭
   ↓
2. 에디터에 기존 내용 로드됨
   ↓
3. 내용 수정
   - 제목, 카테고리, 본문
   - 이미지 추가/삭제
   ↓
4. [🚀 발행하기] 클릭
   ↓
5. 데이터베이스 업데이트
   ↓
6. [← 목록으로] 자동 이동
```

### **시나리오 3: 임시 저장 활용**

```
작성 중...
   ↓
[💾 임시 저장] 클릭
   ↓
브라우저 localStorage에 저장
   ↓
실수로 창 닫음 😱
   ↓
다시 접속
   ↓
localStorage에서 복구 가능!
```

---

## 🎨 Toast UI Editor 고급 기능

### **테이블 삽입**

```
툴바에서 ⊞ 클릭
  ↓
행/열 개수 선택
  ↓
자동 생성:

| Header 1 | Header 2 |
|----------|----------|
| Cell 1   | Cell 2   |
| Cell 3   | Cell 4   |
```

### **코드 블록**

````markdown
툴바에서 < > 클릭
  ↓
언어 선택 (javascript, python, ...)
  ↓
코드 입력:

```javascript
function hello() {
    console.log('Hello World!');
}
```
````

### **인용구**

```markdown
툴바에서 " 클릭
  ↓
> 인용문이 여기에 들어갑니다.
> 여러 줄도 가능합니다.
```

---

## 🔧 문제 해결

### **에디터가 안 보일 때**
1. F12 콘솔 확인
2. Toast UI CDN 로드 확인:
   ```
   https://uicdn.toast.com/editor/latest/toastui-editor-all.min.js
   ```
3. 브라우저 캐시 삭제 (Ctrl + Shift + R)

### **이미지 업로드 실패**
1. Supabase Storage 버킷 확인
2. `archive-images` 버킷 존재 확인
3. 업로드 정책 확인 (관리자 이메일)
4. 파일 크기 확인 (5MB 이하)

### **게시물이 안 보일 때**
1. F12 콘솔에서 Supabase 에러 확인
2. `archive_posts` 테이블 존재 확인
3. RLS 정책 확인

### **수정 버튼을 눌러도 반응 없음**
1. 콘솔에서 JavaScript 에러 확인
2. `admin-toastui.js` 로드 확인
3. Supabase 연결 확인

---

## 📊 파일 구조

```
project/
├── admin.html                 (구버전 - 백업)
├── admin-toastui.html         (신버전 - Toast UI) ⭐
├── index.html
├── post.html
├── css/
│   ├── style.css
│   └── admin-toastui.css      (신규) ⭐
├── js/
│   ├── admin.js               (구버전)
│   ├── admin-toastui.js       (신규) ⭐
│   ├── main.js
│   └── post.js
└── ...
```

---

## 🎯 주요 개선 사항

### **Before (구버전)**
```
❌ 단순 textarea
❌ 마크다운 미리보기 없음
❌ 이미지 업로드 버튼만
❌ 포맷팅 제한적
❌ 게시물 목록 "없습니다"만 표시
```

### **After (Toast UI)**
```
✅ 전문 마크다운 에디터
✅ 실시간 Split View 미리보기
✅ 드래그 & 드롭 이미지 업로드
✅ 다양한 포맷팅 도구
✅ 완전한 게시물 관리 시스템
```

---

## 🚀 다음 단계

### **추천 워크플로우**
1. `admin-toastui.html` 접속
2. 기존 글 목록 확인
3. Toast UI Editor로 새 글 작성 테스트
4. 이미지 드래그 & 드롭 테스트
5. 마크다운/WYSIWYG 모드 전환 테스트
6. 임시 저장 기능 테스트

### **추가 개선 예정**
- 📸 Ctrl+V 이미지 붙여넣기
- 🔍 고급 검색 (본문 검색, 태그)
- 📊 작성 통계 (글자 수, 이미지 수)
- 📱 모바일 최적화
- 🎨 에디터 테마 커스터마이징

---

## 🎉 완성!

이제 완전한 Toast UI Editor 기반 게시글 관리 시스템을 사용하실 수 있습니다!

**핵심 기능:**
- ✅ Toast UI Editor 통합
- ✅ 게시물 CRUD 완전 복구
- ✅ 이미지 업로드 자동화
- ✅ 검색 및 필터링
- ✅ 실시간 미리보기

즐거운 글쓰기 되세요! ✍️
